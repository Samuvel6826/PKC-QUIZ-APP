Need to create whole project with only single files for 3 languages, Change the colour for every round. Change the timer for Rapid Round from 60 sec to 45 sec, Make a div firstly and then add an event listner for it. And give a function as an action for that event listner. This help’s to hide and show particular round. Add functions for every single round.Abbreviation round answers are wrong. Add transparent acore board and it needs to visible for every round. Change the path from game page to rules page in index page. Make a redirection button as home button for every round.